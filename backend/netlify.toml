[build]
  command = "npm install"
  functions = "netlify/functions"
  publish = "public"

# Semua /api/* diarahkan ke fungsi Express (path digandakan lalu dinormalisasi di api.js)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/api/:splat"
  status = 200
